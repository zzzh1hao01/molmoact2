"""Rerun (0.34.1) tooling for the bimanual YAM: URDF viz, takes, catalog, export.

Runs on the dedicated `.venv-rerun` interpreter, NOT the repo's main `.venv`
(see yam_rerun/README.md). Keep this package importable without torch/transformers.
"""
